/** @type {import('tailwindcss').Config} */
// Tailwind CSS v4 no longer requires `content`.
// Keep minimal config for theme extension and plugins.
module.exports = {
  theme: {
    extend: {},
  },
  plugins: [],
};
